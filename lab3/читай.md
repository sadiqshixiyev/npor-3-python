# Лабораторная работа: Рекурсивная генерация бинарных деревьев

**Выполнил:** Шыхыев Садиг Вусал Оглы

---

## Цель работы
Освоение рекурсивного построения бинарных деревьев и изучение структур из модуля `collections` (`namedtuple`, `deque`).

## Реализация

### 1. Базовая структура (`dict`)
Использует вложенные словари с ключами `value`, `left`, `right`.  
Левый потомок вычисляется как `x * 2`, правый — `x + 3`.  
Базовый случай рекурсии — `height <= 0`.

```python
def gen_bin_tree(height, root, left_leaf, right_leaf):
    if height <= 0:
        return None
    return {
        "value": root,
        "left": gen_bin_tree(height - 1, left_leaf(root), left_leaf, right_leaf),
        "right": gen_bin_tree(height - 1, right_leaf(root), left_leaf, right_leaf)
    }
2. Альтернативные структуры (collections)
Тип	Особенность	Применение
namedtuple	Неизменяемый узел, доступ через точку	tree.left.value
deque	Быстрая вставка/удаление с концов	Сериализация дерева в плоский список (BFS)
Тестирование (unittest)
Результаты прогона 5 тестов:

text
test_base_case_zero_height ........... ok
test_tree_height_one ................. ok
test_variant_1_logic ................. ok
test_namedtuple_structure ............ ok
test_deque_serialization ............. ok

Ran 5 tests in 0.002s — OK
Сценарии проверки:
Высота 0: возвращается None.

Высота 1: только корень, потомки None.

Логика вычислений (height=2, root=1):

Левое поддерево: 1 * 2 = 2

Правое поддерево: 1 + 3 = 4

Namedtuple: доступ tree.left.value == 2, попытка изменения вызывает AttributeError.

Deque: обход в ширину дал [1, 2, 4] — корень и два листа.

Вывод
Рекурсивный подход с лямбда-функциями позволяет гибко генерировать древесные структуры.
namedtuple защищает узлы от изменений, deque удобен для линеаризации иерархии при обходе в ширину.
Все алгоритмы протестированы и работают корректно.
